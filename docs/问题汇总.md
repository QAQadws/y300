问题1
漫画章节解析不应该在当前tid中的连续直链解析后就停止，因为当前tid并不能获取全部的章节的链接，有可能是只有在这个tid之前的章节而不包括新章节，若有目录，应调用一次解析目录，若没有目录应搜索解析一次
总之不应该在解析连续跳转链接后就停止，因为可能章节解析不全，如果你有更好的方案按照你的来
目前收藏页的shelf_page的列表模式无法显示图片（可以调用漫画模块和小说模块的封面）

问题2
在第一次使用应用时，若该账号有大量收藏时可能会网络请求然后解析很久很久，所以应该在第一次使用应用时的解析收藏显示类似进度条的东西，形式由你决定