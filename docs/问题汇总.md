仔细阅读当前目录的lib的代码
目前有如下问题
```
1.
优化收藏的解析
小说不急把每个章节解析出来（先不急把小说所在的帖子的每条信息都先解析了，先记住是在这个帖子就好比如title和tid），漫画也不急把每个封面图片解析出来，以免卡顿收藏的解析，先做的应该是快速大概解析收藏得到一个解析好的收藏页并得到相应的数据然后再解析漫画和小说
解析过程中不应等到解析全部的漫画和小说然后手动刷新漫画小说模块才能看到漫画和小说，应该是解析了几个后自动刷新漫画小说模块，然后在解析结束的时候还要再自动刷新一次
然后收藏列表的顺序也很重要，收藏列表的json是按照收藏时间排序的，在前面的是最新收藏的帖子，所以在首次解析过程中也应严格按照新收藏帖子在前，当然这可以被后续的自定义分类给调整掉，但是但又有新加入的收藏时，他是漫画或者小说的话一定出现在漫画或者是小说的“默认”分类的最前端，若不是漫画或者小说它也至少在收藏模块的“默认”的最前端
2.
解析调整
请你注意目前的漫画解析策略有一个问题就是你忽略的“搜索”使用的严格10s一次的限制，导致用户在第一次打开app然后解析他的收藏时会出现有的漫画根本没出现完整的章节，我认为你应该实现一个队列将没有“目录”而且tag为“長篇連載”的漫画加入队列，每10.5s对漫画执行一次更新，然后pop出队列
希望你记得保存这个队列的稳定性，比如用户退出app后又回来的话队列不受影响，还有就是我希望你显示提供信息给用户目前的状态，比如还有多少漫画未完全解析（未完全解析的意思是，该漫画是没有“目录”而且tag为“長篇連載”的漫画只进行了直接跳转连接的解析），预计多少时间完成（未完全解析漫画*10.5s）,不过预计多少时间完成应该是动态的，因为当收藏了同个漫画的多个帖子可能会触发合并机制，导致时间可以稍微缩短
3.
论坛实现收藏
api为
https://bbs.yamibo.com/api/mobile/index.php?module=favthread&version=4
data可能为
data: <String, String>{
    'formhash': formhash,
    'id': 123456,
    'favoritesubmit': 1
其中id要填写的是帖子的tid
其他形式内容请仿照lib\features\reply\data\discuz_reply_api_repository.dart
4.
实时刷新
我认为应该实现实时的刷新的机制，不过我还没有完善论坛模块，但是我希望你预留接口当我在论坛模块收藏了帖子时（收藏帖子是一个api，类似https://bbs.yamibo.com/api/mobile/index.php?module=favthread&version=4）然后你就刷新收藏模块（因为我收藏后https://bbs.yamibo.com/api/mobile/index.php?module=myfavthread&version=4&page=1应该会同步更新），然后解析后刷新对应的漫画或者小说模块，也就是说我想收藏帖子和收藏，漫画，小说模块的同步更新的效果，而不是收藏了之后还要去相应的页面手动再刷新一次
同时若自定义了漫画标题也会触发漫画的更新
5.
检测漫画重复
因为在discuz网页中一个漫画可能有很多的帖子，这导致如果对一个漫画的不同话的帖子进行了收藏，可能导致在解析后的漫画模块中出现这两个分开的同一漫画，请你尝试解决这个问题，比如检测匹配章节的tid，不检测标题是因为即使是同一漫画但每个帖子的标题格式可能不相同
检测漫画重复和合并应发生在三种情况
1.用户第一次使用app，这时会解析大量收藏列表的帖子，若一个漫画的tid包含在一个完全解析后的漫画（完全解析是指有"目录"或者使用了“搜索”来更新的策略）中应该将这个漫画踢出，只保留目前完全解析后的漫画
2.用户不是第一次使用app但是在论坛模块又收藏了一个帖子，收藏后收藏模块会刷新，然后收藏的帖子被解析，若解析后是漫画且它的部分tid已经在漫画模块中则将解析后比漫画模块中对应漫画多出的tid加入到已经存在的漫画，
这样做是因为用户收藏的帖子可能是已经收藏的漫画的最新章节，使用解析后可能章节数比已经收藏的对应漫画还要多，可是已经收藏的对应漫画可能被自定义标题之类的所以应该保留已经收藏的对应漫画，然后在已经收藏的对应漫画中添加最新的章节
3.以上策略出问题时（比如标题解析错误，导致“搜索”策略不可用），用户会自己尝试自定义新添加漫画的标题，自定义后会自动尝试刷新，若刷新后它的章节的部分tid已经在漫画模块中则将解析后比漫画模块中对应漫画多出的tid加入到已经存在的漫画，理由同第二种情况
```
请你在docs文件夹下创建一个文件夹，然后在文件夹下放置多个分阶段的MarkDown来解决这些问题，
一定要把方案尽可能的详细，写得工程化，不要把全部的都写得太过耦合，要尽可能解耦和可维护
因为这是我实现的关键，拜托了，写的MarkDown一定要尽可能的详细并标注该阶段预计所花时间


未登录直接刷新会崩溃









1.
优化收藏的解析
小说不急把每个章节解析出来（先不急把小说所在的帖子的每条信息都先解析了，先记住是在这个帖子就好比如title和tid），漫画也不急把每个封面图片解析出来，以免卡顿收藏的解析，先做的应该是快速大概解析收藏得到一个解析好的收藏页并得到相应的数据然后再解析漫画和小说
解析过程中不应等到解析全部的漫画和小说然后手动刷新漫画小说模块才能看到漫画和小说，应该是解析了几个后自动刷新漫画小说模块，然后在解析结束的时候还要再自动刷新一次
然后收藏列表的顺序也很重要，收藏列表的json是按照收藏时间排序的，在前面的是最新收藏的帖子，所以在首次解析过程中也应严格按照新收藏帖子在前，当然这可以被后续的自定义分类给调整掉，但是但又有新加入的收藏时，他是漫画或者小说的话一定出现在漫画或者是小说的“默认”分类的最前端，若不是漫画或者小说它也至少在收藏模块的“默认”的最前端
2.
解析调整
请你注意目前的漫画解析策略有一个问题就是你忽略的“搜索”使用的严格10s一次的限制，导致用户在第一次打开app然后解析他的收藏时会出现有的漫画根本没出现完整的章节，我认为你应该实现一个队列将没有“目录”而且tag为“長篇連載”的漫画加入队列，每10.5s对漫画执行一次更新，然后pop出队列
希望你记得保存这个队列的稳定性，比如用户退出app后又回来的话队列不受影响，还有就是我希望你显示提供信息给用户目前的状态，比如还有多少漫画未完全解析（未完全解析的意思是，该漫画是没有“目录”而且tag为“長篇連載”的漫画只进行了直接跳转连接的解析），预计多少时间完成（未完全解析漫画*10.5s）,不过预计多少时间完成应该是动态的，因为当收藏了同个漫画的多个帖子可能会触发合并机制，导致时间可以稍微缩短
3.
论坛实现收藏
api为
https://bbs.yamibo.com/api/mobile/index.php?module=favthread&version=4
data可能为
data: <String, String>{
    'formhash': formhash,
    'id': 123456,
    'favoritesubmit': 1
其中id要填写的是帖子的tid
其他形式内容请仿照lib\features\reply\data\discuz_reply_api_repository.dart
4.
实时刷新
我认为应该实现实时的刷新的机制，不过我还没有完善论坛模块，但是我希望你预留接口当我在论坛模块收藏了帖子时（收藏帖子是一个api，类似https://bbs.yamibo.com/api/mobile/index.php?module=favthread&version=4）然后你就刷新收藏模块（因为我收藏后https://bbs.yamibo.com/api/mobile/index.php?module=myfavthread&version=4&page=1应该会同步更新），然后解析后刷新对应的漫画或者小说模块，也就是说我想收藏帖子和收藏，漫画，小说模块的同步更新的效果，而不是收藏了之后还要去相应的页面手动再刷新一次
同时若自定义了漫画标题也会触发漫画的更新
5.
检测漫画重复
因为在discuz网页中一个漫画可能有很多的帖子，这导致如果对一个漫画的不同话的帖子进行了收藏，可能导致在解析后的漫画模块中出现这两个分开的同一漫画，请你尝试解决这个问题，比如检测匹配章节的tid，不检测标题是因为即使是同一漫画但每个帖子的标题格式可能不相同
检测漫画重复和合并应发生在三种情况
1.用户第一次使用app，这时会解析大量收藏列表的帖子，若一个漫画的tid包含在一个完全解析后的漫画（完全解析是指有"目录"或者使用了“搜索”来更新的策略）中应该将这个漫画踢出，只保留目前完全解析后的漫画
2.用户不是第一次使用app但是在论坛模块又收藏了一个帖子，收藏后收藏模块会刷新，然后收藏的帖子被解析，若解析后是漫画且它的部分tid已经在漫画模块中则将解析后比漫画模块中对应漫画多出的tid加入到已经存在的漫画，
这样做是因为用户收藏的帖子可能是已经收藏的漫画的最新章节，使用解析后可能章节数比已经收藏的对应漫画还要多，可是已经收藏的对应漫画可能被自定义标题之类的所以应该保留已经收藏的对应漫画，然后在已经收藏的对应漫画中添加最新的章节
3.以上策略出问题时（比如标题解析错误，导致“搜索”策略不可用），用户会自己尝试自定义新添加漫画的标题，自定义后会自动尝试刷新，若刷新后它的章节的部分tid已经在漫画模块中则将解析后比漫画模块中对应漫画多出的tid加入到已经存在的漫画，理由同第二种情况

解析规则增加

实现小说阅读器

discuz插件开发与测试
重构论坛
先实现首页UI，以及帖子列表UI，帖子详细页应考虑将message的html重构转换
新增与漫画小说模块的联动
实现收藏
实现评分
实现回复消息页

漫画阅读器的detial页的封面用压缩过的图片会不会效果更好，shelf中的封面用压缩过的图片效果呢，有必要改进吗



漫画的更新策略应该是
如果有电梯，目录的跳转链接则优先使用并解析
若没有上述跳转链接，则使用"搜索"（搜索被论坛严格限制了，10s内只能进行一次），搜索的fid为30的第一个帖子应该就是最新的帖子，若这帖子里面有往期章节的跳转链接则使用这些跳转连接更新漫画章节
若最新的帖子中只有一个递归链接，则使用递归策略
然后更新成功或者失败要基于反馈给用户

旧漫画解析

自定义章节


