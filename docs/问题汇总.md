问题1
unified_shelf_page.dart中列表模式的分类横向滚动栏为什么是透明的，导致可以看到后面的列表，还有列表模式时没有图片就不显示图片，不要显示一个奇怪的图片表示没有图片

问题2
收藏页的刷新不要全部从来刷新一遍，先简单比对当前收藏和https://bbs.yamibo.com/api/mobile/index.php?module=myfavthread&version=4&page=1的全部page是否有差异，要不然我全部重新刷新会导致很卡的，而且这个网页的收藏是按照时间顺序降序的，所以最后收藏的在最前面，根据这个信息可以进一步简短收藏页的刷新时间



仔细阅读.information\mangayomi_lib中的源代码和.information\mihon-0.19.9中的代码和当前目录下的lib的源代码
我现在遇到一个瓶颈就是，我的shelf页面性能并不好，第一次进入时会转圈的等待一会才能进入shelf页，以及进入了后它们的封面读取即不快也不够丝滑，像是随机读取杂乱无章而且又很慢
而且它们切换tab栏时是一种消隐后又出现的效果，即使这样他的第一面的各种封面的图片也是一直存在，我感受不到它们的加载可能是通过了预加载
你能仔细阅读这些项目，让后对我目前的实现给出优化方案吗，
请给出一个“分阶段优化.md”，要尽可能的详细，因为这是我实现的关键，并给出以及每个简短以及“耗时”，给出的优化方案一定要现代化，工程化，可维护的。
所以给出一个“分阶段优化.md”然后放到当前目录的docs文件夹下