# 开发文档

## 1. 环境准备

### 1.1 连接安卓模拟器

```bash
adb connect 127.0.0.1:5555
```

### 1.2 安装依赖

```bash
flutter pub get
flutter pub deps
```

---

## 2. 本次实现范围（MVP 第 1 批）

对齐 `docs/Discuz-X3.5移动端App实现方案.md` 的“5. 页面拆分与迭代顺序 -> 第 1 批（MVP）”，本次工程化实现如下页面：

1. 启动页/骨架页
2. 论坛首页（板块 + 列表）
3. forumdisplay 页（版块帖子列表，分页）
4. viewthread 页（帖子详情与回复，分页）
5. 登录页

---

## 3. 架构与解耦说明

### 3.1 目录结构（本次新增）

```text
lib/
  app/
    y300_app.dart
  features/
    startup/
      presentation/
        startup_page.dart

    forum/
      data/
        forum_repository.dart
        forum_home_repository.dart
        forum_display_repository.dart
        models/
          forum_index_models.dart
          forum_display_models.dart
      presentation/
        forum_home_state.dart
        forum_home_controller.dart
        forum_home_page.dart
        forum_display_state.dart
        forum_display_controller.dart
        forum_display_page.dart

    thread/
      data/
        thread_repository.dart
        models/thread_detail_models.dart
      presentation/
        thread_detail_state.dart
        thread_detail_controller.dart
        thread_detail_page.dart

    auth/
      data/auth_repository.dart
      presentation/
        login_state.dart
        login_controller.dart
        login_page.dart

  shared/
    widgets/
      app_skeleton.dart
```

### 3.2 分层职责

1. `data` 层：只负责接口调用与 JSON 到模型的映射。
2. `presentation` 层：控制器负责状态流转，页面只负责渲染与交互。
3. `shared` 层：复用骨架组件，避免各页面重复造轮子。
4. `app` 层：应用入口、主题和初始路由。

### 3.3 关键解耦点

1. `ForumRepository` 与 `ForumDisplayRepository` 均使用抽象接口 + Discuz 实现，便于 Mock。
2. 分页页面（forumdisplay/viewthread）使用独立控制器和独立页面状态对象，避免 UI 直接耦合请求细节。
3. 登录页控制器只关心表单与提交状态，登录成功判定交由 `AuthRepository`。
4. 首页新增 `ForumHomeRepository` 聚合层，统一编排 `forumindex + profile + myfavforum`，控制器只做视图映射。
5. 聚合层对增强信息（profile/myfavforum）采用“失败降级”策略，不阻塞 forumindex 主流程。
6. 页面测试通过 Provider 覆盖仓库依赖，不依赖真实网络。

---

## 4. 页面说明

### 4.1 启动页/骨架页

文件：

1. `lib/features/startup/presentation/startup_page.dart`
2. `lib/shared/widgets/app_skeleton.dart`

行为：

1. 展示冷启动骨架，避免白屏。
2. 启动完成后进入论坛首页。

### 4.2 论坛首页（forumindex）

对应接口：

1. `module=forumindex`
2. `module=profile`
3. `module=myfavforum`

文件：

1. `lib/features/forum/data/forum_home_repository.dart`
1. `lib/features/forum/presentation/forum_home_controller.dart`
2. `lib/features/forum/presentation/forum_home_page.dart`

行为：

1. 未登录：仅展示 `forumindex` 分组版块。
2. 已登录：优先展示“我收藏的版块”（`myfavforum`），随后展示 `forumindex` 版块。
3. “我收藏的版块”复用统一 `_ForumCard` 渲染，保持 UI 组件复用。
4. 点击版块进入 forumdisplay 页。
5. 顶部提供登录页入口。

### 4.3 forumdisplay 页（帖子列表）

对应接口：

1. `module=forumdisplay&fid={fid}&page={page}`

文件：

1. `lib/features/forum/data/forum_display_repository.dart`
2. `lib/features/forum/data/models/forum_display_models.dart`
3. `lib/features/forum/presentation/forum_display_state.dart`
4. `lib/features/forum/presentation/forum_display_controller.dart`
5. `lib/features/forum/presentation/forum_display_page.dart`

行为：

1. 首屏加载当前版块帖子。
2. 支持下拉刷新。
3. 支持加载更多分页。
4. 点击帖子进入 viewthread 页。
5. 模型层兼容 `forum_threadlist/threadlist` 与 `tpp/perpage` 字段，避免接口字段差异导致列表空白。

### 4.4 viewthread 页（帖子详情）

对应接口：

1. `module=viewthread&tid={tid}&page={page}`

文件：

1. `lib/features/thread/presentation/thread_detail_state.dart`
2. `lib/features/thread/presentation/thread_detail_controller.dart`
3. `lib/features/thread/presentation/thread_detail_page.dart`

行为：

1. 展示楼层列表。
2. 支持分页加载更多回复。
3. 使用 Html 组件渲染 message 的富文本内容。
4. 将 `[attach]aid[/attach]` 占位替换为附件图片，未命中的图片追加到正文末尾。

### 4.5 登录页

文件：

1. `lib/features/auth/presentation/login_state.dart`
2. `lib/features/auth/presentation/login_controller.dart`
3. `lib/features/auth/presentation/login_page.dart`

行为：

1. 提供用户名密码输入。
2. 提交后显示 loading，避免重复提交。
3. 成功显示欢迎信息，失败展示错误文案。
4. 实际登录逻辑复用 `AuthRepository.login`（网页登录 + profile 校验）。

---

## 5. 测试说明

### 5.1 测试文件

1. `test/features/startup/presentation/startup_page_test.dart`
2. `test/features/forum/presentation/forum_home_page_test.dart`
3. `test/features/forum/data/forum_home_repository_test.dart`
4. `test/features/forum/presentation/forum_display_page_test.dart`
5. `test/features/forum/data/models/forum_display_models_test.dart`
6. `test/features/thread/presentation/thread_detail_page_test.dart`
7. `test/features/thread/data/models/thread_detail_models_test.dart`
8. `test/features/auth/presentation/login_page_test.dart`
9. `test/features/auth/auth_repository_test.dart`
10. `test/widget_test.dart`

### 5.2 覆盖场景

1. 启动页：骨架展示与启动完成回调。
2. 论坛首页：加载态骨架 + 成功态分组列表。
3. 论坛首页（登录态）：`我收藏的版块` 先于普通分组渲染。
4. 论坛首页聚合仓库：forumindex 失败即失败，profile/myfavforum 失败降级。
5. forumdisplay 页：首屏加载 + 加载更多分页。
6. forumdisplay 模型：`forum_threadlist/threadlist` 与 `tpp/perpage` 兼容映射。
7. viewthread 页：首屏楼层展示 + 加载更多回复。
8. viewthread 模型：附件占位替换 + 追加尾部图片。
9. 登录页：登录成功路径 + 登录失败路径。

### 5.3 如何运行测试

运行新增页面测试：

```bash
flutter test test/features/forum/data/forum_home_repository_test.dart
flutter test test/features/forum/presentation/forum_home_page_test.dart
flutter test test/features/forum/data/models/forum_display_models_test.dart
flutter test test/features/forum/presentation/forum_display_page_test.dart
flutter test test/features/thread/presentation/thread_detail_page_test.dart
flutter test test/features/thread/data/models/thread_detail_models_test.dart
flutter test test/features/auth/presentation/login_page_test.dart
```

运行全量测试：

```bash
flutter test
```

运行静态检查：

```bash
flutter analyze
```

### 5.4 预期结果

1. 测试输出 `All tests passed!`
2. 分析输出 `No issues found!`

---

## 6. 本次改动文件索引

### 页面与控制器

1. `lib/features/forum/presentation/forum_home_state.dart`（新增收藏分组类型）
2. `lib/features/forum/presentation/forum_home_controller.dart`（接入首页聚合仓库）
3. `lib/features/forum/presentation/forum_home_page.dart`（登录态优先渲染收藏分组）
4. `lib/features/forum/presentation/forum_display_page.dart`
5. `lib/features/forum/presentation/forum_display_controller.dart`
6. `lib/features/forum/presentation/forum_display_state.dart`
7. `lib/features/thread/presentation/thread_detail_page.dart`
8. `lib/features/thread/presentation/thread_detail_controller.dart`
9. `lib/features/thread/presentation/thread_detail_state.dart`
10. `lib/features/auth/presentation/login_page.dart`
11. `lib/features/auth/presentation/login_controller.dart`
12. `lib/features/auth/presentation/login_state.dart`

### 数据层

1. `lib/features/forum/data/forum_home_repository.dart`
2. `lib/features/forum/data/forum_display_repository.dart`
3. `lib/features/forum/data/models/forum_display_models.dart`
4. `lib/features/thread/data/models/thread_detail_models.dart`

### 测试

1. `test/features/forum/data/forum_home_repository_test.dart`
2. `test/features/forum/presentation/forum_home_page_test.dart`
3. `test/features/forum/presentation/forum_display_page_test.dart`
4. `test/features/forum/data/models/forum_display_models_test.dart`
5. `test/features/thread/presentation/thread_detail_page_test.dart`
6. `test/features/thread/data/models/thread_detail_models_test.dart`
7. `test/features/auth/presentation/login_page_test.dart`

---

## 7. 下一步建议

1. 在 viewthread 页扩展富文本能力（引用、代码块）。
2. 在 forumdisplay 页加入排序/筛选（最新、精华、热帖）。
3. 登录成功后接入会话恢复和“我的页面”联动刷新。

---

## 漫画模块增量开发（阶段0 + 阶段1）

### 目标

1. 建立漫画域基础骨架（阶段0），不破坏现有论坛/帖子功能。
2. 在帖子详情首楼实现漫画候选识别与解析（阶段1）。
3. 在识别为漫画候选时展示“加入书架”入口（阶段1，先接内存假仓库）。

### 新增目录与文件

1. `lib/features/comic/domain/models/comic_models.dart`
2. `lib/features/comic/domain/services/comic_detector.dart`
3. `lib/features/comic/domain/services/comic_services_impl.dart`
4. `lib/features/comic/data/comic_parser_service.dart`
5. `lib/features/comic/data/comic_repository.dart`
6. `lib/features/comic/data/comic_providers.dart`
7. `lib/features/comic/presentation/widgets/add_to_shelf_button.dart`

### 线程详情接入点改动

1. `lib/features/thread/presentation/thread_detail_state.dart`
   - 新增漫画候选状态字段：`comicCandidateInfo`。
   - 新增解析结果字段：`parsedComicPost`。
   - 新增书架状态字段：`isInShelf`、`isComicActionLoading`。
2. `lib/features/thread/presentation/thread_detail_controller.dart`
   - 首楼加载成功后执行 `ComicDetector + ComicParserService`。
   - 根据解析结果写入页面状态。
   - 增加 `addToShelf()`，阶段1使用内存仓库实现幂等加入。
3. `lib/features/thread/presentation/thread_detail_page.dart`
   - 仅在首楼且判定漫画候选时显示“加入书架/已在书架”按钮。
   - 保持原有帖子正文渲染与分页行为。

### 识别策略（阶段1）

评分阈值：`>= 60` 视为漫画候选。

1. `fid == 30`：+40
2. 正文 `<img>` 数量 >= 2：+35
3. 标题命中关键词（如“第x话/汉化/xx组”）：+20
4. 正文章节链接（`thread-xxx-1-1.html`）数量 >= 2：+15
5. 正文弱关键词（目录/图源/嵌字/校对）：+10

### 解析能力（阶段1）

1. 提取 `img[src]`，去重并过滤常见表情/小图资源路径。
2. 提取 `a[href]`，规范化 URL（去 query/fragment）。
3. 提取章节链接及章节标题（数字或“第x话”）。
4. 提取目录链接与摘要文本。
5. 通过文本正则推断作者/汉化信息（可为空）。

### 依赖变更

1. `pubspec.yaml` 新增：`html: ^0.15.6`

### 新增/更新测试文件

1. `test/features/comic/domain/services/comic_detector_test.dart`
2. `test/features/comic/data/comic_parser_service_test.dart`
3. `test/features/thread/presentation/thread_detail_page_test.dart`（新增漫画入口断言）

### 测试执行说明

本次改动已补齐测试代码，但按协作约定，本轮未执行：

1. `flutter test`
2. `flutter analyze`

请在你本地执行后将结果回传，我会基于你的测试输出继续修复。

---

## 漫画模块增量开发（阶段2：本地存储与书架域）

### 阶段目标

完成“加入书架 -> 漫画Tab可见”的最小闭环，并保证数据持久化。

### 本次架构实现

1. 存储层采用 `sqflite`，在 `features/comic/data/local` 中建立本地数据库模块。
2. 仓库接口 `ComicRepository` 继续作为唯一入口，UI/控制器不直接依赖 SQL。
3. 新增应用主壳 `MainShellPage`，将论坛与漫画作为并列 Tab，避免论坛页与漫画页互相耦合。

### 数据库建模（阶段2）

新增表：

1. `comics`
2. `episodes`
3. `episode_images`
4. `categories`
5. `shelf_items`

说明：

1. 首次建库会自动插入默认分类 `default`（名称“默认”）。
2. `shelf_items` 对 `(category_id, comic_id)` 建立唯一约束，保证重复加入幂等。
3. 为章节/图片/书架排序建立索引以降低后续查询成本。

### 书架域实现

1. `LocalComicRepository` 实现：
   - `isInShelf`
   - `addToShelf`
   - `getShelfItems`
2. `addToShelf` 使用事务：
   - upsert 漫画主信息
   - upsert 章节信息
   - 写入首楼图片索引
   - 幂等写入书架映射

### UI 与导航接入

1. 启动页跳转目标改为 `MainShellPage`。
2. `MainShellPage` 提供“论坛/漫画”双 Tab。
3. 新增 `ComicShelfPage`（阶段2最小版）：
   - 空态提示
   - 基础网格渲染
   - 重试入口

### 线程详情改动

1. `ThreadDetailPageState` 新增 `fid` 字段。
2. `ThreadDetailController.addToShelf` 写入时使用真实 `fid`，不再使用占位值。

### 新增依赖

1. `sqflite`
2. `sqflite_common_ffi`（用于仓库层单测）

### 本次新增/更新测试

1. `test/features/comic/data/local_comic_repository_test.dart`
   - 校验加入书架后可查询
   - 校验重复加入幂等
2. `test/features/comic/domain/comic_shelf_models_test.dart`
3. `test/features/startup/presentation/main_shell_page_test.dart`
4. `test/features/comic/presentation/comic_shelf_page_test.dart`
5. `test/features/startup/presentation/startup_page_test.dart`
   - 新增默认跳转到 `MainShellPage` 的断言

### 测试执行声明

本轮仅补齐实现与测试文件，按协作约定未执行：

1. `flutter test`
2. `flutter analyze`

请在本地执行并回传测试结果，我会继续跟进修复。

---

## 漫画模块增量修复（阶段2后的规则增强）

### 本次修复目标

1. 新增漫画判定/解析规则：当二楼为楼主且二楼以图片为主时，将一楼与二楼共同作为漫画解析输入。
2. 调整书架封面文案展示：标题覆盖在封面底部，最多两行，超出时以“···”结尾。

### 规则增强实现（判定 + 取图）

新增服务：

1. `lib/features/comic/domain/services/comic_post_aggregation_service.dart`

职责：

1. 聚合漫画候选输入，减少控制器中的规则耦合。
2. 封装“楼主一二楼合并”策略：
   - 始终纳入首楼；
   - 二楼同作者且“图片主导”（图片>=2 且图片数>=链接数）时并入。

接入点：

1. `lib/features/thread/presentation/thread_detail_controller.dart`
   - 控制器改为先调用聚合服务，后执行 `ComicDetector + ComicParserService`。
   - 保持控制器职责聚焦于流程编排，不直接持有复杂策略细节。

### 书架卡片样式修复

文件：

1. `lib/features/comic/presentation/comic_shelf_page.dart`

改动：

1. 书名改为覆盖在封面底部（渐变遮罩）。
2. 文本限制两行。
3. 超出时使用自定义截断逻辑，第二行末尾拼接“···”。

### 新增/更新测试

1. 新增 `test/features/comic/domain/services/comic_post_aggregation_service_test.dart`
   - 覆盖“同楼主二楼补图合并”
   - 覆盖“不同作者不合并”
2. 更新 `test/features/comic/presentation/comic_shelf_page_test.dart`
   - 增加标题“···”样式断言
3. 更新 `test/features/thread/presentation/thread_detail_page_test.dart`
   - 增加二楼补图场景回归用例

### 测试声明

按协作约定，本轮仅完成代码与测试文件编写，未执行：

1. `flutter test`
2. `flutter analyze`

---

## 登录链路增强（稳定性与登录态校验）

### 改造目标

1. 解决登录偶发“卡住/超时”体验差的问题。
2. 登录成功后，不只依赖 profile，增加 forumindex 的 auth 字段校验。
3. 登录成功后立即刷新论坛首页数据，保证 UI 登录态及时更新。

### 主要实现

1. `AuthRepository` 新增 `verifyAuthByForumIndex()`：
   - 调用 `module=forumindex`。
   - 判断 `Variables.auth` 是否非空。
2. `AuthRepository.login()` 改为双重校验：
   - 网页登录成功后，先验证 `forumindex.auth`；
   - 再拉取 `profile` 生成 `SessionInfo`。
3. `LoginController.submit()` 增强：
   - 增加并发提交保护，避免重复点击造成链路堆叠；
   - 增加超时兜底（18秒），超时时给出明确错误文案；
   - `_current` 改为 `state.asData?.value ?? initial`，避免状态切换时空值歧义。
4. `LoginPage` 行为增强：
   - 登录成功后 `invalidate(forumHomeControllerProvider)` 触发首页刷新；
   - 成功提示后 `Navigator.pop(true)` 返回上一页。

### 涉及文件

1. `lib/features/auth/data/auth_repository.dart`
2. `lib/features/auth/presentation/login_controller.dart`
3. `lib/features/auth/presentation/login_page.dart`

### 测试更新

1. `test/features/auth/auth_repository_test.dart`
   - 新增 forumindex(auth) 分支校验；
   - 覆盖“网页登录成功但 auth 为空 -> unauthorized”。
2. `test/features/auth/presentation/login_page_test.dart`
   - 同步登录页面文案与仓库接口变更。

### 说明

本轮按协作约定未执行：

1. `flutter test`
2. `flutter analyze`

---

## 漫画模块增量开发（阶段3：书架完整 UI 与分类系统）

### 阶段目标

完成阶段3核心能力：
1. 书架 AppBar 提供标题、搜索入口、菜单入口。
2. 分类系统支持：默认分类 + 新建 + 重命名 + 删除（默认分类保护）。
3. 书架网格列数支持 2/3/4 列切换并持久化。
4. 支持封面替换（自定义 URL）与恢复默认封面。
5. 支持漫画在分类间移动，删除分类时自动回落到默认分类。

### 架构与解耦实现

1. `ComicRepository` 扩展为阶段3统一门面接口，新增分类、显示设置、封面管理、分类迁移能力。
2. `LocalComicRepository` 聚合所有 SQLite 细节，`presentation` 层不直接依赖 SQL。
3. `ComicShelfController` 负责状态编排，新增 `ComicShelfViewState` 聚合：
   - 分类列表
   - 当前分类
   - 当前分类漫画列表
   - 网格列数设置
4. `ComicShelfPage` 仅消费状态并触发用户动作，不持有仓储/数据库细节。

### 本地数据库变更

1. `ComicLocalDb.dbVersion` 从 `1` 升级到 `2`。
2. 新增 `settings` 表保存书架展示配置。
3. 新增升级迁移：旧库升级时自动建 `settings` 表并写入默认 `grid_column_count=3`。

### 关键业务规则

1. 默认分类（`default`）不可重命名、不可删除。
2. 删除自定义分类时：该分类中的漫画会自动迁移到默认分类（幂等处理）。
3. 网格列数强约束在 `[2, 4]` 区间。
4. 封面替换支持：
   - 保存自定义 URL
   - 清空后回退到默认封面（`COALESCE(custom, default)`）

### 本次新增/更新测试

1. `test/features/comic/data/local_comic_repository_test.dart`
   - 新建分类并移动漫画
   - 删除分类回落默认
   - 封面替换与网格配置持久化
2. `test/features/comic/presentation/comic_shelf_page_test.dart`
   - 分类条渲染
   - 标题两行截断样式回归
3. `test/features/comic/domain/comic_shelf_models_test.dart`
   - 新增分类模型与展示配置模型断言
4. `test/features/startup/presentation/main_shell_page_test.dart`
   - 漫画 Tab 文案回归（书架）
5. `test/features/thread/presentation/thread_detail_page_test.dart`
   - 适配 `ComicRepository` 新接口（fake repository）

### 测试执行声明

按协作约定，本轮仅完成代码与测试文件编写，未执行：
1. `flutter test`
2. `flutter analyze`

请在你本地执行后把结果给我，我会继续根据输出修复。

---

## 漫画模块阶段3 UI增强（分类文字导航 + 滑动分页）

### 本次目标

在已有阶段3能力基础上，完成书架分类区交互重构：
1. 分类栏改为纯文字风格（去除边框/删除图标等冗余元素）。
2. 分类栏底部增加圆角小长条指示器，支持随滑动联动动画。
3. 分类内容区改为 `PageView`，支持手势左右滑动切换分类。
4. 滑动过程中可见左右相邻分类内容，避免生硬跳切。
5. 分类管理操作统一收敛到 AppBar 菜单（新建/重命名/删除）。

### 工程化实现与解耦

1. `ComicShelfController` 状态模型升级为 `itemsByCategory` 结构：
   - 按分类缓存列表，支持 `PageView` 即时渲染相邻页。
   - `selectedCategoryId` 仅表示当前选中页，UI 通过 `selectedIndex` 映射。
2. `ComicShelfPage` 升级为 `ConsumerStatefulWidget`：
   - 使用 `PageController` 管理页面滑动与顶部分类联动。
   - 分类栏、指示器、分页内容拆分为独立私有组件，减少耦合。
3. 顶部分类栏改为 `_CategoryPagerHeader`：
   - 文本标签 + 动态指示器。
   - 指示器通过 `AnimatedBuilder` 监听 `PageController.page` 连续位移。
4. 分类页内容抽象为 `_CategoryShelfPage`：
   - 仅关注当前分类网格渲染与空态。
   - 卡片长按操作保持独立组件处理。

### 关键交互说明

1. 点击分类文字：`animateToPage` 平滑滚动。
2. 手势左右滑动：`onPageChanged` 回写 `selectedCategoryId`。
3. AppBar 菜单：
   - `新建分类`
   - `重命名当前分类`（默认分类受保护）
   - `删除当前分类`（默认分类受保护）
   - `每行 2/3/4 列`

### 新增/更新测试

1. `test/features/comic/presentation/comic_shelf_page_test.dart`
   - 分类头、分页容器、指示器存在性
   - 长标题两行省略样式回归
   - `PageView` 横向滑动切换分类场景

### 测试执行声明

按协作约定，本轮仅完成代码与测试文件编写，未执行：
1. `flutter test`
2. `flutter analyze`

请在你本地执行后把结果发我，我继续跟进修复。

### 增量补充：分类栏固定4槽位规则

本次进一步调整分类栏布局规则：
1. 分类标签宽度固定为分类区域的 `1/4`。
2. 当分类数量小于4时，每个分类仍保持 `1/4` 宽度（右侧留白）。
3. 当分类数量大于4时，分类条可横向滚动，默认可见4个分类。
4. 指示器宽度与位置基于固定槽位计算，并跟随 `PageView` 滑动联动。

实现说明：
1. 新增 `_FixedSlotCategoryHeader` 独立组件承载固定槽位逻辑。
2. 使用内部 `ScrollController` 管理横向滚动，并在分类切换时自动滚动到可视范围。
3. 保持 `ComicShelfPage` 与分类渲染细节解耦，仅通过参数驱动。

测试补充：
1. 新增固定宽度断言：在 `400` 宽度下，分类标签宽度约等于 `100`。
2. 新增超4分类滚动断言：初始不可见第5分类，拖动后可见。

---

## 漫画模块阶段4实现（漫画详情与章节管理）

### 阶段目标对齐

实现阶段4核心闭环：
1. 从书架进入漫画详情页。
2. 漫画详情展示：封面、标题、作者、章节统计。
3. 章节列表按倒序展示并可点击进入阅读占位页。
4. 提供“刷新章节”动作：拉取源贴首楼并解析章节链接，合并入本地章节表。

### 工程化设计与解耦

1. 新增详情领域模型：
   - `ComicDetail`
   - `ComicEpisodeItem`
   - `ComicEpisodeRefreshResult`
2. `ComicRepository` 扩展详情与章节能力：
   - `getComicDetail`
   - `getComicEpisodes`
   - `mergeEpisodesFromLinks`
3. 数据层与网络层解耦：
   - `LocalComicRepository` 仅处理本地持久化与合并逻辑。
   - 新增 `ComicEpisodeRefreshService` 负责调用 `ThreadRepository` 拉取首楼并交给 `ComicParserService` 解析章节链接。
4. 表现层职责清晰：
   - `ComicDetailController` 负责详情页状态编排与刷新流程。
   - `ComicDetailPage` 负责渲染与交互，点击章节进入阶段5前的阅读占位页。

### 新增文件

1. `lib/features/comic/domain/models/comic_detail_models.dart`
2. `lib/features/comic/presentation/controllers/comic_detail_controller.dart`
3. `lib/features/comic/presentation/comic_detail_page.dart`

### 关键改动文件

1. `lib/features/comic/data/comic_repository.dart`
2. `lib/features/comic/data/local_comic_repository.dart`
3. `lib/features/comic/domain/services/comic_services_impl.dart`
4. `lib/features/comic/presentation/comic_shelf_page.dart`（书架卡片点击进入详情）

### 阶段4行为说明

1. 书架点击漫画卡片 -> 进入 `ComicDetailPage`。
2. 章节默认倒序显示（最新在上）。
3. 点击右上角刷新：
   - 拉取源贴首楼
   - 解析章节链接
   - 以 `comicId:sourceTid` 幂等合并章节
   - 页面提示新增/更新统计
4. 点击章节进入阅读占位页（阶段5再替换为正式阅读器）。

### 测试新增/更新

1. 新增 `test/features/comic/presentation/comic_detail_page_test.dart`
   - 详情头部与章节列表渲染
   - 刷新章节动作触发与提示文案
2. 更新 `test/features/comic/data/local_comic_repository_test.dart`
   - 详情查询
   - 章节倒序读取
   - 章节合并新增/更新
3. 适配接口变更的测试文件：
   - `test/features/comic/presentation/comic_shelf_page_test.dart`
   - `test/features/thread/presentation/thread_detail_page_test.dart`
   - `test/features/startup/presentation/main_shell_page_test.dart`

### 测试执行声明

按协作约定，本轮仅完成代码与测试文件编写，未执行：
1. `flutter test`
2. `flutter analyze`

请在你本地执行并回传结果，我会继续基于输出修复。

---

## 漫画模块阶段5增量开发（阅读器与图片缓存优化）

### 本次实现范围
1. 正式接入 `ComicReaderPage`，替换阶段4中的阅读占位页。
2. 阅读器支持纵向长图阅读、占位加载、失败重试。
3. 增加手动缓存动作：`缓存本话`、`缓存全部未读`。
4. 新增阅读进度持久化：记录最后阅读章节、图片索引、滚动偏移。
5. 新增章节图片索引与缓存状态持久化接口，保证后续可扩展离线能力。

### 架构与解耦
1. `ComicRepository` 扩展阅读器相关接口（图片索引、缓存状态、阅读进度），UI 不直接依赖 SQL。
2. `NetworkComicReaderService` 负责“网络抓图 + 缓存执行”，`ComicReaderController` 负责状态编排。
3. `ComicReaderPage` 仅消费控制器状态并触发交互动作，保持展示层纯净。

### 数据层升级
1. `comic_local_db.dart` 升级到 `dbVersion=3`。
2. 新增 `reading_progress` 表。
3. `LocalComicRepository` 新增：
   - `getEpisodeImages/saveEpisodeImages`
   - `updateEpisodeImageCacheStatus`
   - `updateLastReadProgress/getLastReadProgress`

### 新增依赖
1. `cached_network_image: ^3.4.1`
2. `flutter_cache_manager: ^3.4.1`

### 关键文件
1. `lib/features/comic/presentation/comic_reader_page.dart`
2. `lib/features/comic/presentation/controllers/comic_reader_controller.dart`
3. `lib/features/comic/domain/services/comic_services_impl.dart`
4. `lib/features/comic/data/comic_repository.dart`
5. `lib/features/comic/data/local_comic_repository.dart`
6. `lib/features/comic/data/local/comic_local_db.dart`
7. `lib/features/comic/presentation/comic_detail_page.dart`

### 测试补充
1. 新增 `test/features/comic/presentation/comic_reader_page_test.dart`
2. 更新 `test/features/comic/data/local_comic_repository_test.dart`（阅读进度与缓存状态落库场景）
3. 适配多个 Fake Repository 以匹配 `ComicRepository` 新接口

### 测试执行声明
本轮仅完成代码与测试文件编写，未执行：
1. `flutter test`
2. `flutter analyze`

---

## 漫画阅读器迁移（阶段0）增量记录（2026-05-01）

### 本轮目标

在不改变现有阅读行为的前提下，完成阶段0要求：
1. 新建阅读器偏好模型（本地持久化）
2. 新建阅读器偏好 Provider/Repository（shared_preferences）
3. 阅读页结构预埋为三层容器（内容层 + 顶部层 + 底部层）
4. 阅读控制器补充通用接口（跳图索引、上下话定位）

### 设计与解耦说明

1. 模型层
- 新增 `ReaderPreferences` 与 `ReaderModePreference`，作为稳定配置对象。

2. 存储层
- 新增 `ReaderPreferencesRepository` 抽象接口。
- 新增 `SharedPrefsReaderPreferencesRepository` 作为当前落地实现。
- 未来替换为 DB/配置中心时无需改 UI 与控制层调用。

3. 状态层
- 新增 `ReaderPreferencesController`（Riverpod AsyncNotifier）。
- 提供 `setReaderMode/setShowPageIndicator/setFullscreenOnOpen/setCacheDirectoryPath` 独立方法，避免调用方散落存储细节。

4. 阅读页结构层
- `ComicReaderPage` 重构为 `Stack` 三层：
  - 内容层：图片流与提示
  - 顶部层：阶段0占位（Key：`comic-reader-top-overlay`）
  - 底部层：章节动作区（Key：`comic-reader-bottom-overlay`）
- 通过层化结构为阶段2菜单动效直接预留扩展点，避免后续大重构。

5. 控制器接口层
- `ComicReaderController` 新增：
  - `jumpToImageIndex(int index, {double? scrollOffset})`
  - `goToPreviousEpisode()`
  - `goToNextEpisode()`
- 该接口作为后续进度条拖动、章节切换的通用能力入口。

### 本轮新增/修改文件

新增：
1. `lib/features/comic/presentation/models/reader_preferences.dart`
2. `lib/features/comic/presentation/providers/reader_preferences_provider.dart`
3. `test/features/comic/presentation/models/reader_preferences_test.dart`
4. `test/features/comic/presentation/providers/reader_preferences_provider_test.dart`
5. `test/features/comic/presentation/controllers/comic_reader_controller_test.dart`

修改：
1. `lib/features/comic/presentation/comic_reader_page.dart`
2. `lib/features/comic/presentation/controllers/comic_reader_controller.dart`
3. `test/features/comic/presentation/comic_reader_page_test.dart`

### 测试补充（已编写，未执行）

1. `reader_preferences_test.dart`
- 默认值验证
- `clearCacheDirectoryPath` 行为验证

2. `reader_preferences_provider_test.dart`
- 空存储加载默认值
- 更新后重新读取持久化值

3. `comic_reader_page_test.dart`
- 保持原有阅读页关键节点断言
- 新增三层容器占位节点断言

4. `comic_reader_controller_test.dart`
- 覆盖阶段0通用接口依赖的章节顺序前提（用于后续接口行为扩展）

### 执行声明

按协作约定，本轮仅完成代码与测试文件编写，未执行：
1. `flutter test`
2. `flutter analyze`

请你本地执行后把结果给我，我再继续按结果修复。

### 阶段0修订（生命周期异常 + 菜单交互基线）

补充原因：
- 发现 `ComicReaderController` 在 autoDispose 场景下出现 `UnmountedRefException`。
- 菜单交互需对齐需求：默认隐藏，点击中部才动效显示，上一话/下一话随菜单出现。

本次修订实现：
1. `ComicReaderController` 生命周期安全改造
- 依赖改为在 `build()` 时读取并缓存字段，避免异步回调中访问已销毁 `ref`。
- 关键 async 流程的 `await` 后新增 `ref.mounted` 检查。

2. `ComicReaderPage` 菜单交互改造
- 去掉固定可见的顶部/底部菜单。
- 新增中部点击区 `comic-reader-center-tap-zone`。
- 顶部/底部菜单改为 `SlideTransition` 动效显示。
- `上一话/下一话` 按钮归属底部菜单，仅菜单可见时出现。

3. 测试更新
- `comic_reader_page_test.dart` 新增中部点击后菜单控件可见断言。

本轮仍未执行（按约定）：
1. `flutter test`
2. `flutter analyze`

---

## 漫画阅读迁移阶段1续做（2026-05-01）

### 本轮目标

在已有阶段1代码基础上，继续补齐工程化细节与可回归测试，重点确保：
1. “更多”页与缓存设置页文案可读且稳定（修复乱码文案）。
2. 阶段1关键交互具备页面级测试覆盖。
3. 登录入口迁移回归有独立用例保障（论坛页不再出现登录入口）。

### 本轮实现

1. `MorePage` 文案修复与结构保持
   - 修复 `AppBar` 与列表项标题/副标题乱码。
   - 保持登录入口、缓存目录入口、阅读设置占位、关于占位的解耦结构不变。

2. `CacheSettingsPage` 与 `CacheSettingsController` 文案修复
   - 修复页面标题、错误提示、按钮文案与状态提示文案乱码。
   - 保持控制器职责边界：页面仅消费状态，不承载存储/文件选择细节。

3. 阶段1测试补充
   - 新增 `cache_settings_page_test.dart`：覆盖默认目录展示、自定义目录选择、恢复默认目录三条主链路。
   - 新增 `forum_home_login_entry_migration_test.dart`：回归验证论坛首页 `AppBar` 无登录入口。
   - 更新 `more_page_test.dart`：增加中文文案断言，确保入口文案可见性回归。

### 本轮新增/更新文件

新增：
1. `test/features/more/presentation/cache_settings_page_test.dart`
2. `test/features/forum/presentation/forum_home_login_entry_migration_test.dart`

修改：
1. `lib/features/more/presentation/more_page.dart`
2. `lib/features/more/presentation/cache_settings_page.dart`
3. `lib/features/more/presentation/cache_settings_controller.dart`
4. `test/features/more/presentation/more_page_test.dart`

### 测试执行声明

按协作约定，本轮仅完成代码与测试文件编写，未执行：
1. `flutter test`
2. `flutter analyze`

请你本地执行后把结果发我，我会基于结果继续修复。

---

## 漫画阅读迁移阶段2实现记录（2026-05-02）

### 本轮目标
1. 完成“点击中部唤起/隐藏菜单”的核心交互。
2. 将阅读页菜单交互按组件拆分，避免页面文件持续膨胀。
3. 保持现有业务行为不回退（缓存动作、章节切换返回值约定）。

### 工程化拆分
1. 新增 `ReaderTapZones`：负责点击分区交互，不承载业务逻辑。
2. 新增 `ReaderTopBar`：负责顶部菜单渲染（返回、章节标题、缓存动作入口）。
3. 新增 `ReaderBottomPanel`：负责底部菜单渲染（上一话/下一话）。
4. `ComicReaderPage` 聚焦于页面编排：
   - 统一组装内容层 + 交互层 + 菜单层
   - 统一管理菜单显隐动画状态
   - 通过控制器接口触发业务动作

### 关键行为说明
1. 菜单默认隐藏。
2. 点击中部区域切换顶部/底部菜单显示状态。
3. 上下菜单通过 `SlideTransition` 平滑滑入/滑出。
4. 菜单隐藏时通过 `IgnorePointer` 禁止菜单层交互，避免无效点击。

### 本轮新增/修改文件
新增：
1. `lib/features/comic/presentation/widgets/reader_tap_zones.dart`
2. `lib/features/comic/presentation/widgets/reader_top_bar.dart`
3. `lib/features/comic/presentation/widgets/reader_bottom_panel.dart`
4. `test/features/comic/presentation/widgets/reader_tap_zones_test.dart`

修改：
1. `lib/features/comic/presentation/comic_reader_page.dart`

### 本轮测试文件（已编写，未执行）
1. `test/features/comic/presentation/comic_reader_page_test.dart`（既有回归）
2. `test/features/comic/presentation/widgets/reader_tap_zones_test.dart`（新增分区交互单测）

### 执行声明
按协作约定，本轮未执行：
1. `flutter test`
2. `flutter analyze`

请你在本地执行后把输出给我，我再根据结果继续修复。

---

## 漫画阅读迁移阶段3实现记录（2026-05-02）

### 本轮目标
1. 完成阅读模式三态切换：`vertical` / `ltr` / `rtl`。
2. 页面渲染能力升级为双轨：`ListView` + `PageView`。
3. 模式切换时按“逻辑图片 index”对齐进度，避免丢页。
4. 模式切换结果持久化并在下次打开时自动恢复。

### 工程化实现与解耦
1. 底部菜单组件增强：
   - `ReaderBottomPanel` 新增 `SegmentedButton` 模式切换入口。
   - 组件仅负责渲染与回调，不承载业务状态。
2. 阅读页组装层增强：
   - `ComicReaderPage` 新增双轨内容渲染：
     - 垂直模式：`_buildVerticalReaderView`（`ListView`）
     - 分页模式：`_buildPagedReaderView`（`PageView.builder`）
   - 引入 `_onReaderModeChanged` 封装模式切换流程。
3. 进度对齐策略：
   - 统一以“当前逻辑页 index”为真值。
   - 切换模式前解析当前 index，切换后调用 `jumpToImageIndex` 持久化对齐。
4. 持久化策略：
   - 继续复用 `ReaderPreferencesController.setReaderMode`。
   - 页面初始化时读取 `readerPreferencesControllerProvider`，按偏好自动选择渲染轨道。

### 关键交互说明
1. `vertical`：图片长流连续阅读。
2. `ltr`：分页阅读，`PageView(reverse: false)`。
3. `rtl`：分页阅读，`PageView(reverse: true)`。
4. 分页模式左右分区点击：
   - `ltr`：左侧上一页，右侧下一页。
   - `rtl`：左侧下一页，右侧上一页。

### 本轮新增/修改文件
新增：
1. `test/features/comic/presentation/widgets/reader_bottom_panel_test.dart`

修改：
1. `lib/features/comic/presentation/comic_reader_page.dart`
2. `lib/features/comic/presentation/widgets/reader_bottom_panel.dart`
3. `test/features/comic/presentation/comic_reader_page_test.dart`

### 本轮测试文件（已编写，未执行）
1. `comic_reader_page_test.dart`
   - 阶段2基础渲染回归
   - 持久化为 `ltr` 时使用分页轨道
   - 菜单切换到 `rtl` 后切换到分页轨道
2. `reader_bottom_panel_test.dart`
   - 模式切换控件回调正确性

### 执行声明
按协作约定，本轮未执行：
1. `flutter test`
2. `flutter analyze`

请你在本地执行后把输出给我，我再按结果继续修复。

### 阶段3回归修复（2026-05-02，分页模式布局溢出）

#### 现象（来自测试回归）
1. `ComicReaderPage uses paged renderer when persisted mode is ltr` 失败。
2. `ComicReaderPage switches from vertical to rtl mode via bottom switch` 失败。
3. 报错一致：`RenderFlex overflowed by 487 pixels on the bottom`，定位到 `comic_reader_page.dart` 分页渲染分支。

#### 根因分析
1. 分页模式下，每页由 `PageView` 提供固定视口高度约束。
2. 旧实现复用了纵向模式的 `Column + CachedNetworkImage(fitWidth)`，图片按宽度铺满后高度可能超过单页可用高度。
3. 在固定高度页内继续纵向堆叠导致 `RenderFlex overflow`。

#### 修复方案
1. 为分页模式提供独立渲染分支，不再复用纵向布局：
   - 使用 `SizedBox.expand` 绑定单页视口约束。
   - 图片改为 `BoxFit.contain`，保证在单页内完整适配。
   - 异常态使用 `Center + Column(mainAxisSize: min)`，避免高度膨胀。
2. 纵向模式保持原有 `fitWidth` 行为不变，避免影响既有阅读体验。

#### 本轮修改文件
1. `lib/features/comic/presentation/comic_reader_page.dart`

#### 执行声明
按协作约定，本轮仅修复代码并更新文档，未执行：
1. `flutter test`
2. `flutter analyze`

---

## 漫画阅读迁移阶段4实现记录（2026-05-02）

### 本轮目标
1. 实现底部“可拖动阅读进度条 + 两侧章节按钮”。
2. 支持拖动预览页码、释放后执行跳页。
3. 保持垂直/分页模式下跳页行为一致且可维护。

### 工程化拆分
1. 新增 `ReaderProgressBar` 组件：
   - 负责进度条区块结构与基础交互回调。
   - 包含：左外“上一话”、右外“下一话”、中间胶囊与当前页/总页。
2. `ReaderBottomPanel` 职责升级：
   - 组合 `ReaderProgressBar` + 模式切换控件。
   - 仅转发回调，不处理业务状态。
3. `ComicReaderPage` 负责业务编排：
   - `onProgressChanged`：仅更新本地预览页码。
   - `onProgressChangeEnd`：根据阅读模式执行实际跳页并持久化。

### 交互策略
1. `onChanged`：更新 `_sliderPreviewIndex`，用于菜单内即时页码反馈。
2. `onChangeEnd`：
   - `vertical`：根据章节总滚动范围换算 offset，并 `jumpTo`。
   - `ltr/rtl`：`PageController.jumpToPage`。
   - 两类模式都会调用 `jumpToImageIndex`，保持逻辑进度落库一致。

### 边界处理
1. 无图片场景由页面上游拦截（保留空章节提示）。
2. 单页场景：Slider `divisions = null`，避免 `0 divisions` 异常。
3. `current/total` 展示做了最小值保护，避免非法页码显示。

### 本轮新增/修改文件
新增：
1. `lib/features/comic/presentation/widgets/reader_progress_bar.dart`
2. `test/features/comic/presentation/widgets/reader_progress_bar_test.dart`

修改：
1. `lib/features/comic/presentation/widgets/reader_bottom_panel.dart`
2. `lib/features/comic/presentation/comic_reader_page.dart`
3. `test/features/comic/presentation/comic_reader_page_test.dart`
4. `test/features/comic/presentation/widgets/reader_bottom_panel_test.dart`

### 执行声明
按协作约定，本轮未执行：
1. `flutter test`
2. `flutter analyze`

请你本地执行并回传结果，我再按结果继续修复。

---

## 阅读器进度跳转性能优化（2026-05-02）

### 问题背景
用户反馈进度条跳转存在卡顿和偶发不跳转，怀疑与图片懒加载、跳转时机、频繁落库有关。

### 本轮优化目标
1. 提高进度条跳转命中率（减少“点了不跳”）。
2. 减少跳转后白屏/等待感（提升图片就绪概率）。
3. 控制滚动进度落库频率，避免与主线程交互造成卡顿。

### 工程化方案
1. 控制器层：滚动进度持久化防抖
   - `onScrollProgress` 改为“先更新内存态 + 防抖落库”。
   - 新增 `_scheduleProgressPersistence`（180ms）集中写库。
   - 降低高频滚动场景下 DB 写入压力。

2. 服务层：增加批量预加载抽象
   - `ComicReaderService` 新增 `prefetchImages(List<String>)` 默认实现。
   - `jumpToImageIndex` 后触发“目标页附近窗口预加载”（半径2页）。
   - 设计为接口默认方法，兼容既有实现与 fake。

3. 页面层：垂直模式跳转稳定性增强
   - `_jumpVerticalToIndex` 由一次跳转改为“双帧确认”。
   - 首次按当前 maxScroll 跳转；下一帧根据最新 maxScroll 做纠偏。
   - 解决图片延迟布局时 offset 计算失真的问题。

4. 页面层：进度提交节流
   - `onChangeEnd` 增加 120ms 重复提交门控。
   - 避免手势边界抖动导致重复提交、状态震荡。

### 关键改动文件
1. `lib/features/comic/domain/services/comic_services_impl.dart`
2. `lib/features/comic/presentation/controllers/comic_reader_controller.dart`
3. `lib/features/comic/presentation/comic_reader_page.dart`
4. `test/features/comic/presentation/controllers/comic_reader_controller_test.dart`

### 测试补充（已写，未执行）
1. `jumpToImageIndex` 触发近邻预加载窗口。
2. `onScrollProgress` 防抖写库行为（最终值正确落库）。

### 执行声明
按协作约定，本轮未执行：
1. `flutter test`
2. `flutter analyze`

请你本地执行后回传结果，我再继续按结果优化（包括窗口大小、节流时长、跳转纠偏参数）。

### 编译修复补充（2026-05-02）

本轮根据你回传的 `flutter test / flutter analyze` 结果，修复 `ComicReaderService.prefetchImages` 的接口实现缺失问题。

修复内容：
1. `NetworkComicReaderService` 显式实现 `prefetchImages`。
2. `comic_reader_page_test.dart` 中 `_ReaderFakeService` 显式实现 `prefetchImages`。

说明：
- 这是 `implements` 语义下的必需实现修复，不改变既有业务逻辑。
- 目的是恢复全量编译与测试可运行状态。

### 测试用例修复补充（2026-05-02）

根据回归结果，`onScrollProgress persists with debounce` 用例失败的根因是：
1. `comicReaderControllerProvider` 为 `autoDispose`。
2. 测试中未保持 provider 订阅，导致防抖计时期间 provider 被回收。
3. `onDispose` 触发后定时器取消，最终无写库记录。

修复方式：
- 在该测试里增加 `container.listen(...)` 持有订阅，并在 `tearDown` 释放。
- 仅修测试生命周期，不修改线上防抖逻辑。

---

## 进度条防抽搐稳健性优化（2026-05-02）

### 问题现象
进度条点击/拖动后偶发“来回抽搐并回到原位”，导致首次提交不生效或视觉回弹明显。

### 根因定位
1. `Slider` 的本地交互状态与 provider 异步状态更新发生竞争。
2. 跳转提交期间，`_restorePositionIfNeeded` 仍可能回写旧位置，造成反向回弹。
3. 滑块在提交窗口内仍可继续触发交互，增加状态震荡概率。

### 本轮工程化方案
1. 引入滑块交互状态机（页面层）
   - `drag session`：`_isSliderSessionActive`
   - `commit in-flight`：`_isSliderCommitInFlight`
   - `pending target`：`_pendingCommittedIndex`
2. 提交阶段冻结交互（组件层）
   - `ReaderProgressBar` 新增 `interactionLocked`
   - 锁定时禁用 `onChangeStart/onChanged/onChangeEnd`
3. 提交期间冻结位置恢复（页面层）
   - `_restorePositionIfNeeded` 在 `commit in-flight` 时直接跳过
   - 避免旧状态把新目标覆盖回去
4. 目标达成后解锁
   - 在分页 `onPageChanged` 或提交末尾调用 `_tryReleaseSliderCommitLock`
   - 成功对齐后统一清理 preview 和锁状态

### 关键改动文件
1. `lib/features/comic/presentation/comic_reader_page.dart`
2. `lib/features/comic/presentation/widgets/reader_progress_bar.dart`
3. `lib/features/comic/presentation/widgets/reader_bottom_panel.dart`
4. `test/features/comic/presentation/comic_reader_page_test.dart`
5. `test/features/comic/presentation/widgets/reader_progress_bar_test.dart`
6. `test/features/comic/presentation/widgets/reader_bottom_panel_test.dart`

### 测试补充（已写，未执行）
1. `reader_progress_bar_test.dart`
   - 锁定状态下滑块不可交互（`onChanged` 不触发）
2. `comic_reader_page_test.dart`
   - 拖动提交期间进度条关键节点稳定可见，不发生立刻回弹消失

### 执行声明
按协作约定，本轮未执行：
1. `flutter test`
2. `flutter analyze`

### 测试稳定性修复补充（2026-05-02）

根据回归失败信息，`ComicReaderPage keeps slider stable during jump commit` 用例存在 pending timer。

修复内容：
1. 将垂直纠偏中的 `Future.delayed(16ms)` 改为 `WidgetsBinding.instance.endOfFrame`。
2. 移除未使用字段 `_isSliderSessionActive`，清理 analyze warning。

说明：
- 此改动不改变业务交互语义，仅优化帧同步方式与测试环境稳定性。

---

## �����Ķ�Ǩ�ƽ׶�5ʵ�ּ�¼��2026-05-02��
### ����Ŀ��
1. Ϊ�����Ķ�ģʽͳһ��������������˫ָ���� + ˫�����ţ���
2. ������ͼƬ���š��͡���ҳ��ҳ/�����ҳ�����Ƴ�ͻ��������ҳ��
3. ����ҳ��㡢�����ְ�������ں���������ع顣

### ���̻�ʵ�������
1. ���� `ReaderZoomableImage` ���������ְ��
- �ļ���`lib/features/comic/presentation/widgets/reader_zoomable_image.dart`
- ��װ `InteractiveViewer + TransformationController`��
- ֧��˫�����ţ�1x/2x �л����������Ŷ�����
- ֧��ͨ�� `onZoomStateChanged` ���ⱨ�浱ǰ�Ƿ��ڷŴ�̬��

2. `ComicReaderPage` �����������ͻЭ��
- �ļ���`lib/features/comic/presentation/comic_reader_page.dart`
- ͼƬ��Ⱦͳһ���� `ReaderZoomableImage`������ҳ����д����ϸ�ڡ�
- ������ͼƬ����ά��������״̬ӳ�䣺`_zoomedStateByIndex`��
- ������ͼƬ���ڷŴ�̬ʱ��
  - ��ҳģʽ���� `PageView` ������`NeverScrollableScrollPhysics`����
  - ���õ���������������ҵ����ҳ����
- �����Żָ� 1x ���Զ��ָ���ҳ��������������

3. ������������չ�ɿؿ���
- �ļ���`lib/features/comic/presentation/widgets/reader_tap_zones.dart`
- ���� `enabled` ���������������뿪�أ������ҵ�����

### ��������/�޸��ļ�
������
1. `lib/features/comic/presentation/widgets/reader_zoomable_image.dart`
2. `test/features/comic/presentation/widgets/reader_zoomable_image_test.dart`

�޸ģ�
1. `lib/features/comic/presentation/comic_reader_page.dart`
2. `lib/features/comic/presentation/widgets/reader_tap_zones.dart`
3. `test/features/comic/presentation/comic_reader_page_test.dart`
4. `test/features/comic/presentation/widgets/reader_tap_zones_test.dart`

### ���Բ��䣨��д��δִ�У�
1. `reader_zoomable_image_test.dart`
- ����˫��������״̬�л��ص����Ŵ� -> ��ԭ����

2. `comic_reader_page_test.dart`
- �����Ķ�ҳ�� `ReaderZoomableImage` ���ػع顣

3. `reader_tap_zones_test.dart`
- ���� `enabled=false` ʱ������������������ص���

### ִ������
��Լ��������δִ�У�
1. `flutter test`
2. `flutter analyze`

---

## �����Ķ�Ǩ�ƽ׶�6ʵ�ּ�¼��2026-05-02��
### ����Ŀ��
1. �������� > ����Ŀ¼���á�������ͨ������ͼƬ������·��
2. ͳһ `cacheCurrentEpisode` / `cacheAllUnread` / Ԥ����Ļ���д��·����
3. �� `cacheLocalPath` �� URL ����Ϊ��ʵ�����ļ�·�������ں�������������չ��

### ���̻����������
1. ������ģ��洢������
- ���� `lib/core/config/app_storage_keys.dart`��ͳһά�� `comic_cache_dir`��
- Ŀ�ģ����� settings �� comic ģ���ַ���ɢ���Ư�ơ�

2. ��������Ŀ¼������
- ���� `ComicCacheDirectoryResolver`��`lib/features/comic/data/comic_cache_directory_provider.dart`
- ְ��
  - ��ȡ�Զ���Ŀ¼���á�
  - ����ͳһ��Ŀ¼ `y300_comic_cache`��
  - ȷ��Ŀ¼���ڲ�������Ч·����
- �����ҳ�������㲻ֱ������ `SharedPreferences/path_provider` ϸ�ڡ�

3. �����Զ��建���ļ�ϵͳ�빤����
- ���� `CustomPathFileSystem`��`lib/features/comic/data/custom_cache_file_system.dart`
- ���� `ComicCacheManagerFactory`��`lib/features/comic/data/comic_cache_manager_factory.dart`
- ְ��
  - ���ڽ������Ŀ¼���� `CacheManager(Config(...))`��
  - �������ļ��̶�д��ָ��Ŀ¼�µ� `y300_comic_images` ��Ŀ¼��

4. Provider ����루�첽������ʽ����
- ���� `lib/features/comic/data/comic_providers.dart`��
  - ���� resolver/factory provider��
  - `comicCacheManagerProvider` ��Ϊ `FutureProvider<BaseCacheManager>`��
- Ŀ�ģ�����Ŀ¼������Ȼ�첽��������ҵ�����ʽ������Ӳ����Ĭ��Ŀ¼��

5. �Ķ�����㷵�ر��ػ���·��
- ���� `lib/features/comic/domain/services/comic_services_impl.dart`��
  - `cacheImage` �� `Future<bool>` ����Ϊ `Future<ComicImageCacheResult>`��
  - �������ģ�ͣ�`success + localPath`��
  - ͨ�� `downloadFile` ���� `FileInfo.file.path`��
  - `comicReaderServiceProvider` ��Ϊ `FutureProvider<ComicReaderService>`���ȴ� cache manager ������

6. �����������������
- ���� `lib/features/comic/presentation/controllers/comic_reader_controller.dart`��
  - `build()` ��ͨ�� `await ref.read(comicReaderServiceProvider.future)` ��ȡ���񣨱����������ڰ�ȫ����
  - `retryImage/cacheCurrentEpisode/cacheAllUnread` ȫ���� `ComicImageCacheResult` д�롣
  - `cacheLocalPath` �ɡ�URLռλ������Ϊ����ʵ����·������

7. ����ģ�鸴��ͳһ��
- ���� `lib/features/more/data/more_settings_repository.dart`��
- ��ԭ���ڲ��������л�Ϊ `AppStorageKeys.comicCacheDirectory`��

### ��������/�޸��ļ�
������
1. `lib/core/config/app_storage_keys.dart`
2. `lib/features/comic/data/comic_cache_directory_provider.dart`
3. `lib/features/comic/data/custom_cache_file_system.dart`
4. `lib/features/comic/data/comic_cache_manager_factory.dart`
5. `test/features/comic/data/comic_cache_directory_resolver_test.dart`

�޸ģ�
1. `lib/features/more/data/more_settings_repository.dart`
2. `lib/features/comic/data/comic_providers.dart`
3. `lib/features/comic/domain/services/comic_services_impl.dart`
4. `lib/features/comic/presentation/controllers/comic_reader_controller.dart`
5. `test/features/comic/presentation/controllers/comic_reader_controller_test.dart`
6. `test/features/comic/presentation/comic_reader_page_test.dart`

### ���Բ��䣨��д��δִ�У�
1. `comic_cache_directory_resolver_test.dart`
- ���ǣ������Զ���Ŀ¼�󣬽������ָ���Զ���Ŀ¼�� `y300_comic_cache` ��Ŀ¼����ȷ��Ŀ¼���ڡ�

2. `comic_reader_controller_test.dart`
- ������`cacheCurrentEpisode` �Ὣ������񷵻صı���·��д��ֿ⣨`cacheLocalPath`����

3. ���䣺���ڷ��� provider �� `cacheImage` ǩ����������� fake/override ��ͬ�����졣

### ִ������
��Լ��������δִ�У�
1. `flutter test`
2. `flutter analyze`
